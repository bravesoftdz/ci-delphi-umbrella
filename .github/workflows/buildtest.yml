name: Container Workflow

on:
  push:
    branches:
      - develop

jobs:
  test:
    runs-on: windows-latest
    container:
      image: docker://magicxor/radstudio:xe-delphi-net3.5-ltsc2019

    steps:
      - uses: actions/checkout@v2

      - name: Test unit project
        run: echo "Teste"